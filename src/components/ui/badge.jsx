/**
 * Badge / Chip — app-canonical "badge" primitives backed by HeroUI v3.
 *
 * - <Chip color="primary" variant="flat" size="sm">Active</Chip>
 *     pill-style label (use this for status/category tags).
 * - <Badge> ... </Badge>
 *     notification-dot / count overlay (anchored to another element).
 *
 * `color`: default | primary | secondary | success | warning | danger
 * `variant`: solid | flat | bordered | ...
 */
export { Badge, BadgeAnchor, BadgeLabel, Chip, ChipLabel } from "@heroui/react";
